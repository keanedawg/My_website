
import { appendHtml } from 'cams-website/helpers/append-html';
import { module, test } from 'qunit';

module('Unit | Helper | append html');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = appendHtml([42]);
  assert.ok(result);
});

